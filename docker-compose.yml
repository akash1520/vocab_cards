services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vocab
      POSTGRES_PASSWORD: vocab
      POSTGRES_DB: vocab_cards
    ports:
      - "5433:5432"
    volumes:
      - vocab_pg_data:/var/lib/postgresql/data

volumes:
  vocab_pg_data:
